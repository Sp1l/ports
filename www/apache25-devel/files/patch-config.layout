--- config.layout.orig	2018-03-30 15:43:58 UTC
+++ config.layout
@@ -281,17 +281,17 @@
   bindir:        ${exec_prefix}/bin
   sbindir:       ${exec_prefix}/sbin
   libdir:        ${exec_prefix}/lib
-  libexecdir:    ${exec_prefix}/libexec/apache2
+  libexecdir:    ${exec_prefix}/libexec/apache25
   mandir:        ${prefix}/man
-  sysconfdir:    ${prefix}/etc/apache2
-  datadir:       ${prefix}/www
-  installbuilddir: ${prefix}/share/apache2/build
+  sysconfdir:    ${prefix}/etc/apache25
+  datadir:       ${prefix}/www/apache25
+  installbuilddir: ${prefix}/share/apache25/build
   errordir:      ${datadir}/error
   iconsdir:      ${datadir}/icons
   htdocsdir:     ${datadir}/data
-  manualdir:     ${prefix}/share/doc/apache2
+  manualdir:     ${prefix}/share/doc/apache25
   cgidir:        ${datadir}/cgi-bin
-  includedir:    ${prefix}/include/apache2
+  includedir:    ${prefix}/include/apache25
   localstatedir: /var
   runtimedir:    ${localstatedir}/run
   logfiledir:    ${localstatedir}/log
